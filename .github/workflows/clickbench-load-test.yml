#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: "ClickBench Load Test"

on:
  schedule:
    - cron: '0 6 * * 1'   # placeholder schedule, replace once cadence is decided
  workflow_dispatch:
    inputs:
      input_parquet_glob:
        description: "Override input glob (default = full 100 files)"
        required: false
        default: "s3a://clickhouse-public-datasets/hits_compatible/athena_partitioned/hits_*.parquet"
      connector:
        description: "Connector under test (tags the run)"
        required: false
        default: "spark"
      run_profile:
        description: "Cluster spec profile (drives EMR sizing; recorded on the run)"
        required: false
        default: "small"
        type: choice
        options:
          - small
          - large

concurrency:
  group: clickbench-load-test
  cancel-in-progress: false

jobs:
  load-test:
    runs-on: ubuntu-22.04
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-east-1
      # EMR 7.13.0 ships Spark 3.5.6 + Scala 2.12. As of mid-2026 no EMR 7.x
      # release bundles Spark 4, so the connector jar must be built for
      # 3.5_2.12 to match. Bump this if a Spark-4 EMR release appears.
      EMR_RELEASE_LABEL: emr-7.13.0
      # EMR_INSTANCE_TYPE / EMR_CORE_COUNT are derived from RUN_PROFILE by the
      # "Resolve cluster specs from run profile" step below.
      CH_DATABASE: clickbench
      CH_TABLE: hits
      CH_SECRET_ID: clickbench-load-test/target-ch-password
      INPUT_PARQUET_GLOB: ${{ inputs.input_parquet_glob || 's3a://clickhouse-public-datasets/hits_compatible/athena_partitioned/hits_*.parquet' }}
      # --- Connector operating config UNDER TEST (plan §4, §6.9) ---------------
      # These live in version control and are echoed into perf.runs.runtime (see
      # "Insert run record"), so the config under test is auditable per run and
      # the dashboard config filter can slice on them. This repo is the SINGLE
      # SOURCE OF TRUTH: the diverged S3 orchestrator copy that used to carry
      # batchSize (~line 95 there) is retired — the workflow uploads
      # benchmarks/clickbench_ingest.py fresh every run (see "Upload jar and
      # script to S3" + emr_submit.sh SCRIPT_S3_URI), so these values reach EMR
      # from the repo, not from S3. Contract §1.4 key spellings.
      BATCH_SIZE: "100000"          # spark.clickhouse.write.batchSize (rows/batch); known operating point
      ASYNC_INSERT: "0"             # server async_insert mode OFF at the operating point
      # write_parallelism is RECORD-ONLY: nothing forces it (a repartition would
      # inject a full shuffle of the dataset inside the timed window — a
      # trend-breaking workload change). The write stage runs at its natural
      # input-split partition count (task concurrency is then bounded by
      # executor slots); the ingest job reports the OBSERVED count via an S3
      # sidecar (WRITE_PARALLELISM_META_S3_URI, see "Compute run id and S3
      # paths") and "Insert run record" echoes it into
      # runtime['write_parallelism']. A forcing knob may return with the
      # parallelism-sweep campaign (plan §4 / task #22) as a deliberate,
      # annotated config change — do not reintroduce it silently here.
      PARTITION_SCHEME: "toYear(EventDate)"  # current target DDL (benchmarks/sql/clickbench/02_create_hits.sql); toYYYYMM switch is a later plan §9 step
      DATASET: "hits"               # logical dataset shape (contract §1.4)
      # --- Pinned reference build, arm P (plan §2, open decision 1) -----------
      # The two-arm protocol runs HEAD (arm H) and a PINNED reference (arm P) on
      # the same cluster/hour/server so environment noise cancels in the H/P
      # ratio. Arm P MUST be a FETCHED FROZEN ARTIFACT, never rebuilt from a
      # pinned sha (toolchain drift would make P non-constant and reintroduce
      # the noise it exists to cancel).
      #
      # RELEASE-vs-FROZEN-S3 OUTCOME: a compatible published release EXISTS on
      # Maven Central for this pipeline's build (spark 3.5 / scala 2.12), so we
      # pin the Maven Central release jar directly — no S3 freeze channel is
      # needed. Decision 1 resolved to "last tagged release": v0.10.0.
      #
      # PIN-BUMP PROCEDURE (deliberate, annotated event):
      #   1. Pick the new released version published to Maven Central for
      #      g=com.clickhouse.spark a=clickhouse-spark-runtime-3.5_2.12.
      #   2. Download the jar, compute sha256 (shasum -a 256).
      #   3. Update PINNED_CONNECTOR_VERSION + PINNED_JAR_SHA256 + PINNED_GIT_SHA
      #      (git rev-parse v<version>) here, in ONE annotated commit. The commit
      #      is the dashboard "pin bump" event (v_env_annotations, plan §7).
      # A bump is the ONLY thing that moves arm P; between bumps P is constant.
      PINNED_CONNECTOR_VERSION: "0.10.0"
      # Maven Central coordinates of the frozen arm-P artifact. Group/artifact are
      # fixed by the build (settings.gradle: clickhouse-spark-runtime-3.5_2.12);
      # version is the pin above.
      PINNED_MAVEN_COORDS: "com.clickhouse.spark:clickhouse-spark-runtime-3.5_2.12:0.10.0"
      # sha256 of the fetched jar, verified in the "Fetch pinned reference jar"
      # step. A mismatch FAILS the run (the artifact is not what we pinned).
      PINNED_JAR_SHA256: "6769056dcfe2e798e9027b7a2d29fac4b23cf16f113c500df2fcf138737f6665"
      # git sha of tag v0.10.0 — per-arm provenance recorded on the pinned run
      # (task #13 puts arm/connector_version/git_sha into runtime).
      PINNED_GIT_SHA: "269ac1a33235b507e715912b53a9397450c7f45b"
      # --- Mandatory scope keys on every perf.runs row (contract §1.1) --------
      ENVIRONMENT_CLASS: "production"  # the Spark target is a production Cloud service
      # TARGET_REGION: Cloud region of the TARGET ClickHouse service. Set from
      # repo config, never hardcoded per run (contract §1.1). us-east-1 matches
      # the EMR side; task #9 confirms the ClickHouse target's region via the
      # Cloud console — update here (one place) once confirmed.
      TARGET_REGION: "us-east-1"
      # Run dimensions recorded on perf.runs (and exported to the DWH).
      CONNECTOR: ${{ inputs.connector || 'spark' }}
      RUN_PROFILE: ${{ inputs.run_profile || 'small' }}
      # Integrity-check source ground truth (plan §6.1, contract §2.1). When the
      # run's INPUT_PARQUET_GLOB equals DEFAULT_INPUT_PARQUET_GLOB, these pinned
      # constants ARE rows_expected / unique_expected; run_metrics_sql.py
      # re-derives them via s3() ONLY for non-default (smoke/override) globs —
      # per-run uniqExact over the full ~100M-row source would be a full WatchID
      # scan with GB-scale hash state every run. Both values were derived once
      # with `clickhouse local` over the public glob (count(): 4.8s;
      # uniqExact(WatchID): 73s). NOTE: the source has exactly 4 duplicate
      # WatchIDs (…497 rows vs …493 distinct), which is why duplicate detection
      # is a target-vs-source delta, never count()-uniqExact() on the target.
      # Re-derive both constants if the dataset or default glob changes.
      DEFAULT_INPUT_PARQUET_GLOB: "s3a://clickhouse-public-datasets/hits_compatible/athena_partitioned/hits_*.parquet"
      SOURCE_ROWS_EXPECTED: "99997497"
      SOURCE_UNIQUE_EXPECTED: "99997493"
    steps:
      - uses: actions/checkout@v4

      - name: Install ClickHouse client (clickhouse-connect) + boto3
        run: pip install --quiet clickhouse-connect boto3

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "17"
          cache: gradle

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.CLICKBENCH_AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Build connector runtime jar
        run: |
          ./gradlew -Dspark_binary_version=3.5 -Dscala_binary_version=2.12 \
            :clickhouse-spark-runtime-3.5_2.12:shadowJar --no-daemon

      - name: Fetch pinned reference jar (arm P)
        # Two-arm protocol (plan §2): fetch the FROZEN pinned reference artifact
        # from Maven Central alongside HEAD's shadowJar, so both jars exist in
        # one workflow run with provenance recorded. This step is deliberately
        # INDEPENDENT of the ingest loop (task #13 restructures the loop and
        # consumes this jar via PINNED_JAR_LOCAL / provenance env). It only
        # fetches + verifies; it does not run anything.
        #
        # If a future pin points at a version not on Maven Central (e.g. the
        # pipeline moves to spark-4.0, which has no published release), replace
        # the Maven URL below with a PINNED_JAR_S3_URI fetch from the CI bucket
        # (secrets.CLICKBENCH_S3_BUCKET) — a one-time human upload with a
        # version-stamped immutable key — keeping the sha256 verification.
        run: |
          set -euo pipefail
          MVN_BASE="https://repo1.maven.org/maven2/com/clickhouse/spark/clickhouse-spark-runtime-3.5_2.12/${PINNED_CONNECTOR_VERSION}"
          MVN_JAR="${MVN_BASE}/clickhouse-spark-runtime-3.5_2.12-${PINNED_CONNECTOR_VERSION}.jar"
          DEST="$RUNNER_TEMP/clickhouse-spark-runtime-pinned.jar"
          echo "fetching pinned arm P jar: $MVN_JAR"
          curl -fsSL -o "$DEST" "$MVN_JAR"
          GOT=$(shasum -a 256 "$DEST" | awk '{print $1}')
          if [ "$GOT" != "$PINNED_JAR_SHA256" ]; then
            echo "PINNED JAR SHA256 MISMATCH — refusing to proceed."
            echo "  expected: $PINNED_JAR_SHA256"
            echo "  got:      $GOT"
            echo "The fetched artifact is not the pinned reference. Either Maven"
            echo "Central returned a different/corrupt file, or the pin metadata"
            echo "in the workflow env is stale. Fix via the PIN-BUMP PROCEDURE."
            exit 1
          fi
          echo "pinned jar verified: sha256=$GOT ($PINNED_MAVEN_COORDS)"
          echo "PINNED_JAR_LOCAL=$DEST" >> "$GITHUB_ENV"

      - name: Compute run id and S3 paths
        run: |
          source benchmarks/scripts/lib_runid.sh
          echo "RUN_ID=$RUN_ID" >> "$GITHUB_ENV"
          echo "RUN_START=$RUN_START" >> "$GITHUB_ENV"
          echo "EVENT_LOG_DIR_SPARK=s3a://${{ secrets.CLICKBENCH_S3_BUCKET }}/spark-event-logs/$RUN_ID/" >> "$GITHUB_ENV"
          # EMR 7.13 ships Spark 3.5.6, which writes a single flat event-log
          # file (application_<appId>_1) - rolling format (eventlog_v2_*/) is
          # Spark 4.0+ only. Match the flat file. If we ever move to a Spark-4
          # EMR release, switch this glob to eventlog_v2_*/events_*.
          echo "EVENT_LOG_URI=s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/spark-event-logs/$RUN_ID/application_*" >> "$GITHUB_ENV"
          echo "JAR_S3_URI=s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/jars/$RUN_ID/clickhouse-spark-runtime.jar" >> "$GITHUB_ENV"
          # Pinned reference (arm P) jar rides the same per-run jars/ path with a
          # distinct key so both arms' artifacts are colocated for the run.
          echo "PINNED_JAR_S3_URI=s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/jars/$RUN_ID/clickhouse-spark-runtime-pinned.jar" >> "$GITHUB_ENV"
          echo "SCRIPT_S3_URI=s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/scripts/$RUN_ID/clickbench_ingest.py" >> "$GITHUB_ENV"
          # Sidecar the ingest job writes its OBSERVED write-stage partition
          # count to (write parallelism is record-only, not forced); "Insert
          # run record" reads it back into runtime['write_parallelism'].
          echo "WRITE_PARALLELISM_META_S3_URI=s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/run-meta/$RUN_ID/write_parallelism" >> "$GITHUB_ENV"

      - name: Upload jar and script to S3
        run: |
          # The runtime build/libs may also hold thin -empty/-sources/-javadoc
          # jars; the shaded fat jar is the one we need. Exclude those classifiers
          # and pick the largest (the fat jar dwarfs them) to avoid uploading the
          # wrong artifact.
          JAR=$(ls -S spark-3.5/clickhouse-spark-runtime/build/libs/clickhouse-spark-runtime-3.5_2.12-*.jar \
                | grep -Ev '(sources|javadoc|empty)\.jar$' | head -1)
          aws s3 cp "$JAR" "$JAR_S3_URI"
          # Pinned reference (arm P) jar, fetched + sha256-verified above.
          aws s3 cp "$PINNED_JAR_LOCAL" "$PINNED_JAR_S3_URI"
          aws s3 cp benchmarks/clickbench_ingest.py "$SCRIPT_S3_URI"

      - name: Bootstrap schema (idempotent CREATE IF NOT EXISTS)
        # clickbench.hits lives on the target service; perf.{runs,metrics,
        # ch_inserts} live on the separate metrics service.
        env:
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
        run: python benchmarks/scripts/bootstrap_schema.py

      - name: Capture pre-run server covariates (uptime, RSS, active parts)
        # BEFORE truncate/ingest: point-in-time reads of the target's uptime,
        # RSS and active-part count, recorded as perf.metrics rows for the run
        # (plan §6.3). Captured pre-truncate so pre_run_active_parts reflects the
        # state the run actually inherits. Best-effort: a failure here must not
        # block the run, and these rows are covariates, not gated metrics — so
        # the step does not participate in the capture/rollback gating below.
        continue-on-error: true
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
        run: python benchmarks/scripts/run_metrics_sql.py benchmarks/sql/perf/21_pre_run_covariates.sql

      - name: Truncate target table
        env:
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          CH_DATABASE: clickbench
          CH_TABLE: hits
          SKIP_TRUNCATE: ${{ vars.SKIP_TRUNCATE || '0' }}
        run: python benchmarks/scripts/truncate_target.py

      - name: Resolve cluster specs from run profile
        # To add a "bigger ClickHouse" tier, extend with a per-profile
        # target-CH secret. Chosen specs are recorded on the run (see the
        # runtime map in "Insert run record").
        run: |
          case "$RUN_PROFILE" in
            small) IT=m6i.2xlarge; CC=4 ;;
            large) IT=m6i.4xlarge; CC=8 ;;
            *)     echo "unknown run_profile '$RUN_PROFILE', defaulting to small"; IT=m6i.2xlarge; CC=4 ;;
          esac
          echo "EMR_INSTANCE_TYPE=$IT" >> "$GITHUB_ENV"
          echo "EMR_CORE_COUNT=$CC"   >> "$GITHUB_ENV"
          echo "run_profile=$RUN_PROFILE -> ${CC} x ${IT}"

      - name: Provision EMR
        env:
          EMR_LOG_URI: s3://${{ secrets.CLICKBENCH_S3_BUCKET }}/emr-logs/
          EMR_SUBNET_ID: ${{ secrets.CLICKBENCH_EMR_SUBNET_ID }}
          EMR_EC2_KEY_NAME: ${{ secrets.CLICKBENCH_EMR_KEY_NAME }}
          EMR_SERVICE_ROLE: ${{ secrets.CLICKBENCH_EMR_SERVICE_ROLE }}
          EMR_INSTANCE_PROFILE: ${{ secrets.CLICKBENCH_EMR_INSTANCE_PROFILE }}
        run: |
          CLUSTER_ID=$(benchmarks/scripts/emr_provision.sh)
          echo "CLUSTER_ID=$CLUSTER_ID" >> "$GITHUB_ENV"

      - name: Ensure CH password in Secrets Manager
        env:
          CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
        run: |
          if aws secretsmanager describe-secret --secret-id "$CH_SECRET_ID" >/dev/null 2>&1; then
            echo "Secrets Manager secret $CH_SECRET_ID already exists; leaving it"
          else
            aws secretsmanager create-secret --name "$CH_SECRET_ID" \
              --secret-string "$CH_PASSWORD" >/dev/null
            echo "created Secrets Manager secret $CH_SECRET_ID"
          fi

      - name: Submit Spark step
        id: submit
        env:
          CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          CH_PORT: ${{ secrets.CLICKBENCH_TARGET_CH_PORT }}
          CH_PROTOCOL: https
          CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          # Connector operating config under test — forwarded to the Spark job
          # via emr_submit.sh appMasterEnv so clickbench_ingest.py applies them
          # (plan §6.9). Sourced from the top-level workflow env, not S3.
          # WRITE_PARALLELISM_META_S3_URI (from "Compute run id and S3 paths")
          # is record-only: the job writes its observed write-stage partition
          # count there; nothing forces parallelism.
          BATCH_SIZE: ${{ env.BATCH_SIZE }}
          ASYNC_INSERT: ${{ env.ASYNC_INSERT }}
        run: benchmarks/scripts/emr_submit.sh

      - name: Capture run end timestamp
        if: always()
        run: echo "RUN_END=$(date -u +%Y-%m-%dT%H:%M:%S)" >> "$GITHUB_ENV"

      - name: Teardown EMR
        if: always()
        run: benchmarks/scripts/emr_teardown.sh

      - name: Wait for merges to settle
        if: ${{ !cancelled() && steps.submit.outcome == 'success' }}
        env:
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          CH_DATABASE: clickbench
          CH_TABLE: hits
        run: |
          # wait_for_settle prints only the settle-end timestamp on stdout; the
          # 1/0 timed-out flag comes back via the sidecar status file (its stdout
          # contract can't carry a second value). See plan §6.4.
          export SETTLE_STATUS_FILE="$RUNNER_TEMP/settle_status"
          SETTLE_END=$(benchmarks/scripts/wait_for_settle.py)
          echo "SETTLE_END=$SETTLE_END" >> "$GITHUB_ENV"
          SETTLE_SECONDS=$(( $(date -u -d "$SETTLE_END" +%s) - $(date -u -d "$RUN_END" +%s) ))
          echo "SETTLE_SECONDS=$SETTLE_SECONDS" >> "$GITHUB_ENV"
          SETTLE_TIMED_OUT=$(cat "$SETTLE_STATUS_FILE" 2>/dev/null || echo 0)
          echo "SETTLE_TIMED_OUT=$SETTLE_TIMED_OUT" >> "$GITHUB_ENV"
          echo "settle took ${SETTLE_SECONDS}s, timed_out=${SETTLE_TIMED_OUT} (RUN_END=$RUN_END SETTLE_END=$SETTLE_END)"

      - name: Capture metrics — Spark event log + CH query_log/metric_log/part_log
        id: capture
        if: ${{ !cancelled() && steps.submit.outcome == 'success' }}
        # Order within the loop is irrelevant: 10-14 and 16 are independent
        # INSERTs into perf.metrics, 15 INSERTs into perf.ch_inserts. Both
        # target tables are already created by the bootstrap step.
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          CH_DATABASE: clickbench
          CH_TABLE: hits
        run: |
          for sql in \
            benchmarks/sql/perf/10_insert_from_event_log.sql \
            benchmarks/sql/perf/11_insert_from_query_log.sql \
            benchmarks/sql/perf/12_insert_from_metric_log.sql \
            benchmarks/sql/perf/13_insert_from_part_log.sql \
            benchmarks/sql/perf/14_insert_settle_seconds.sql \
            benchmarks/sql/perf/15_capture_raw_inserts.sql \
            benchmarks/sql/perf/16_insert_throttling.sql \
            benchmarks/sql/perf/17_insert_memory_peaks.sql \
            benchmarks/sql/perf/18_insert_derived_metrics.sql \
            benchmarks/sql/perf/19_insert_rate_stability.sql \
            benchmarks/sql/perf/20_insert_integrity.sql \
            benchmarks/sql/perf/22_insert_settle_timed_out.sql
          do
            echo "=== $sql ==="
            python benchmarks/scripts/run_metrics_sql.py "$sql"
          done
          # run_cost_usd (plan §6.6): EMR instance-hours x in-repo on-demand price.
          # Part of the gated capture so it rolls back with the rest if capture
          # fails and only lands on fully-recorded runs.
          echo "=== run_cost_usd ==="
          python benchmarks/scripts/compute_run_cost.py

      - name: Decide validity-guard flags (flagged-not-failed, contract §1.3)
        id: flags
        # Plan §6.10 / contract §1.3: some conditions make a run NON-COMPARABLE
        # without failing it — they FLAG it so bands/ratios exclude it by default,
        # while capture/export stay complete. Two Spark-relevant guards here:
        #   task_retries   — task_failed_count > 0 (retried work pollutes volume)
        #   settle_timeout — settle right-censored at the 1800s cap
        # (integrity MISMATCH is a separate case — it FAILS the run, handled by
        # the "Verify integrity" step; it is NOT a flag.)
        #
        # The flag must be decided AFTER the facts exist: SETTLE_TIMED_OUT is
        # already in GITHUB_ENV (settle step), and task_failed_count exists as a
        # perf.metrics row only once "Capture metrics" (10_insert_from_event_log)
        # has run — hence this step is gated on capture success and reads that one
        # metric back with a cheap point query by run_id (read_task_failed_count.py).
        # It emits FLAGGED / FLAG_REASON into GITHUB_ENV; the RUNTIME jq assembly in
        # "Insert run record" turns them into runtime['flagged'] / ['flag_reason'].
        if: ${{ !cancelled() && steps.submit.outcome == 'success' && steps.capture.outcome == 'success' }}
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
        run: |
          set -euo pipefail
          TASK_FAILED_COUNT=$(python benchmarks/scripts/read_task_failed_count.py)
          echo "task_failed_count=${TASK_FAILED_COUNT} settle_timed_out=${SETTLE_TIMED_OUT:-0}"
          # Assemble flag_reason tokens in the contract's fixed vocabulary,
          # order = definition order (task_retries before settle_timeout). Join
          # multiples with a single '|' (the ONLY permitted separator — a comma
          # would collide with map-value parsing, contract §1.3).
          REASONS=()
          if [ "${TASK_FAILED_COUNT:-0}" -gt 0 ]; then REASONS+=("task_retries"); fi
          if [ "${SETTLE_TIMED_OUT:-0}" != "0" ]; then REASONS+=("settle_timeout"); fi
          if [ "${#REASONS[@]}" -gt 0 ]; then
            FLAG_REASON=$(IFS='|'; echo "${REASONS[*]}")
            echo "FLAGGED=1" >> "$GITHUB_ENV"
            echo "FLAG_REASON=$FLAG_REASON" >> "$GITHUB_ENV"
            echo "run FLAGGED (non-comparable): flag_reason=$FLAG_REASON"
          else
            # Contract §1.3: flagged is present-iff-tripped — write NEITHER key
            # when no guard trips (do NOT emit flagged='0'). Leaving FLAGGED unset
            # makes the jq assembly below omit both keys entirely.
            echo "run not flagged (no validity guard tripped)"
          fi

      - name: Insert run record
        id: run_record
        # Written AFTER metrics capture, gated on its success — perf.runs only
        # gets a row when the run's metrics fully captured.
        if: ${{ !cancelled() && steps.submit.outcome == 'success' && steps.capture.outcome == 'success' }}
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          GIT_SHA: ${{ github.sha }}
        run: |
          export CONNECTOR_VERSION=$(cat version.txt)
          # write_parallelism is the OBSERVED write-stage partition count the
          # ingest job uploaded to the run's S3 sidecar (record-only — nothing
          # forces it). Fall back to the descriptive token 'input-splits' if
          # the sidecar is missing (upload is best-effort on the EMR side).
          WRITE_PARALLELISM_OBSERVED=$(aws s3 cp "$WRITE_PARALLELISM_META_S3_URI" - 2>/dev/null | tr -cd '0-9')
          WRITE_PARALLELISM_OBSERVED="${WRITE_PARALLELISM_OBSERVED:-input-splits}"
          echo "write_parallelism (observed) = $WRITE_PARALLELISM_OBSERVED"
          # Spark/EMR attrs + connector operating config + mandatory scope keys
          # go into the generic perf.runs.runtime map; another connector would
          # build its own RUNTIME json here. Config keys use contract §1.4
          # shared spellings (batch_size, write_parallelism, async_insert,
          # partition_scheme, dataset); target_region + environment_class are
          # the mandatory scope keys (contract §1.1). spark_version/scala_version/
          # emr_release stay connector-namespaced (contract §1.4).
          # Validity-guard flags (contract §1.3), decided by the "Decide
          # validity-guard flags" step above and passed via GITHUB_ENV. Present
          # iff a guard tripped: FLAGGED is set to '1' with FLAG_REASON only when
          # non-comparable, otherwise BOTH are unset. The jq below adds
          # runtime['flagged'] / runtime['flag_reason'] ONLY when FLAGGED='1', so
          # a clean run carries neither key (never flagged='0') — the contract's
          # present-iff-tripped rule.
          export RUNTIME=$(jq -cn --arg sv 3.5 --arg scv 2.12 --arg emr "$EMR_RELEASE_LABEL" \
            --arg it "$EMR_INSTANCE_TYPE" --arg cc "$EMR_CORE_COUNT" \
            --arg sto "${SETTLE_TIMED_OUT:-0}" \
            --arg bs "$BATCH_SIZE" --arg wp "$WRITE_PARALLELISM_OBSERVED" \
            --arg ai "$ASYNC_INSERT" --arg ps "$PARTITION_SCHEME" \
            --arg ds "$DATASET" \
            --arg ec "$ENVIRONMENT_CLASS" --arg tr "$TARGET_REGION" \
            --arg flagged "${FLAGGED:-}" --arg flag_reason "${FLAG_REASON:-}" \
            '{spark_version: $sv, scala_version: $scv, emr_release: $emr,
              emr_instance_type: $it, emr_core_count: $cc,
              settle_timed_out: $sto,
              batch_size: $bs, write_parallelism: $wp, async_insert: $ai,
              partition_scheme: $ps, dataset: $ds,
              environment_class: $ec, target_region: $tr}
             + (if $flagged == "1"
                then {flagged: "1", flag_reason: $flag_reason}
                else {} end)')
          python benchmarks/scripts/insert_run_record.py

      - name: Roll back this run if it wasn't fully recorded
        # Capture is several independent INSERTs and the run record is a separate
        # step. If capture fails partway, OR capture succeeds but the run-record
        # insert fails, delete this run's rows from perf.* (runs/metrics/ch_inserts)
        # so trends never show metrics without a complete run record. Export is
        # skipped in both cases.
        if: ${{ !cancelled() && steps.submit.outcome == 'success' && (steps.capture.outcome == 'failure' || steps.run_record.outcome == 'failure') }}
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
        run: python benchmarks/scripts/rollback_run_metrics.py

      - name: Export metrics to DWH bucket
        if: ${{ !cancelled() && steps.submit.outcome == 'success' && steps.capture.outcome == 'success' && steps.run_record.outcome == 'success' }}
        # Assumes the cross-account DWH write role (clickbench-load-test-ci is
        # a trusted principal) to stream this run's perf.runs/metrics/ch_inserts
        # to the DWH bucket as Parquet; the DWH side ingests via s3queue.
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
          DWH_ROLE_ARN: ${{ secrets.CLICKBENCH_DWH_ROLE_ARN }}
        run: python benchmarks/scripts/export_metrics_to_dwh.py

      - name: Verify integrity (fail the run on a row-count / duplicate mismatch)
        # Runs AFTER capture + run-record + export so the integrity metrics are
        # already persisted and exported; this step only reads them back and
        # fails the job on a mismatch (contract §3 — mismatch fails outright).
        # Gated on capture AND run-record success: if the run record failed, the
        # rollback step has already wiped this run's perf.* rows, so integrity is
        # not evaluable here (and the job is already red from that failure) — a
        # second "no integrity metrics found" failure would just be noise.
        # Independent of export so a mismatch is surfaced even if export failed.
        if: ${{ !cancelled() && steps.submit.outcome == 'success' && steps.capture.outcome == 'success' && steps.run_record.outcome == 'success' }}
        env:
          METRICS_CH_HOST: ${{ secrets.CLICKBENCH_METRICS_CH_HOST }}
          METRICS_CH_USER: ${{ secrets.CLICKBENCH_METRICS_CH_USER }}
          METRICS_CH_PASSWORD: ${{ secrets.CLICKBENCH_METRICS_CH_PASSWORD }}
        run: python benchmarks/scripts/check_integrity.py

      - name: Truncate target table (cleanup at end of run)
        # Truncate AFTER the write + settle + metrics capture: metrics are read
        # from ClickHouse's system.*_log, not the hits table, so they are
        # unaffected. Leaves the table empty so the next run starts clean.
        if: ${{ !cancelled() }}
        env:
          TARGET_CH_HOST: ${{ secrets.CLICKBENCH_TARGET_CH_HOST }}
          TARGET_CH_USER: ${{ secrets.CLICKBENCH_TARGET_CH_USER }}
          TARGET_CH_PASSWORD: ${{ secrets.CLICKBENCH_TARGET_CH_PASSWORD }}
          CH_DATABASE: clickbench
          CH_TABLE: hits
          SKIP_TRUNCATE: ${{ vars.SKIP_TRUNCATE || '0' }}
        run: python benchmarks/scripts/truncate_target.py
